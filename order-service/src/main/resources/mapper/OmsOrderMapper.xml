<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhou6.cloud.order.mapper.OmsOrderMapper">

    <update id="updateStatus">
        UPDATE oms_order
        SET order_status = #{nextStatus},
            update_by = #{operatorId},
            update_time = NOW()
        WHERE order_sn = #{orderSn}
          AND order_status = #{expectedStatus}
    </update>

    <select id="selectTimeoutOrders" resultType="com.zhou6.cloud.order.entity.OmsOrder">
        SELECT id, order_sn, user_id, total_amount, pay_amount, order_status,
               create_by, create_time, update_by, update_time
        FROM oms_order
        WHERE order_status = #{status}
          AND create_time &lt;= #{deadline}
        ORDER BY create_time ASC
        LIMIT #{limit}
    </select>

</mapper>
